# python-log-analyzer
A Python tool to detect suspicious HTTP requests and brute-force attacks
✨Features
. Brute-force Attack Detection:
Triggre alerts on repeated authentication failures(401 Unauthorized).
. SQL Injection Detection:Scans requests for common SQLi signatures
🚀 Usage
Run the script with python 3:
python analyzer.py
